<%= form_for(@datafield) do |f| %>
  <% if @datafield.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@datafield.errors.count, "error") %> prohibited this datafield from being saved:</h2>

      <ul>
      <% @datafield.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :fieldType %><br />
    <%= f.text_field :fieldType %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
